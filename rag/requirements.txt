# RAG reference implementation — extra dependencies.
#
# This folder is intentionally isolated from the main app. To play with it:
#     python -m pip install -r rag/requirements.txt
#
# Models used here are all FREE / local:
#   - Embeddings: sentence-transformers/all-MiniLM-L6-v2 (~80 MB, CPU-only OK)
#   - Vector store: chromadb (embedded, file-based, no server needed)
#   - LLM (optional): OpenRouter via the openai SDK with a `*:free` model
#     (only used at answer-generation time; retrieval works without it)

# Already in backend/requirements.txt but listed here for the
# folder to be self-installable:
openai>=1.55.0
pypdf>=5.1.0
python-docx>=1.1.2

# RAG-specific:
sentence-transformers>=3.0.0    # local embedding model, free
chromadb>=0.5.0                  # embedded vector DB, file-based
tiktoken>=0.7.0                  # token-aware chunking
